{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window. M0 needs only the core command bridge; all file access happens in Rust, not through a frontend plugin.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
